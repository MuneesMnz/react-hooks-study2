import React from "react";
// import { userContext, MailContext } from "./../../App";

const ComponentC = () => {
  return (
    <div>
      {/* <userContext.Consumer>
        {(user) => {
         return(
          <MailContext.Consumer>
            {
              mail=>{
                return "hello " + user+ " "+ mail.name + " "+mail.email;
              }
            }
          </MailContext.Consumer>
         ) 
        }} */}
      {/* </userContext.Consumer> */}
    </div>
  );
};

export default ComponentC;
